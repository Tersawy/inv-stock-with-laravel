export const all = (state, expenses) => {
	console.log(expenses);
};

export const one = (state, expense) => {
	console.log(expense);
};

export const create = (state, expense) => {
	console.log(expense);
};

export const update = (state, expense) => {
	console.log(expense);
};

export const moveToTrash = (state, expense) => {
	console.log(expense);
};

export const remove = (state, expense) => {
	console.log(expense);
};
